import { ArrowNavigatorDirective } from './arrow-navigator.directive';

describe('ArrowNavigatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowNavigatorDirective();
    expect(directive).toBeTruthy();
  });
});
